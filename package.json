{
  "name": "analoguserexperience",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "echo 'No dev server required for static HTML.'",
    "build": "echo 'Static site; no build step.'",
    "vercel-build": "echo 'Static site; no build step.'"
  }
}
